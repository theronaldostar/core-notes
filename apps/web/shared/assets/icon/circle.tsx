import type { SVGProps } from "react";

const Circle = (props: SVGProps<SVGSVGElement>) => (
	<svg width={37} height={38} viewBox="0 0 37 38" {...props}>
		<circle cx={18.377} cy={18.86} r={18.354} />
	</svg>
);

export { Circle };
